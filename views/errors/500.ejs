<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title><%= statusCode || 500 %> • FixOra Services</title>

  <script src="https://cdn.tailwindcss.com"></script>

  <link rel="preconnect" href="https://fonts.googleapis.com" />
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
  <link
    href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap"
    rel="stylesheet"
  />

  <style>
    body {
      font-family: 'Inter', sans-serif;
    }
  </style>
</head>

<body class="min-h-screen bg-gray-50 flex items-center justify-center px-4">

  <div class="max-w-lg w-full bg-white rounded-2xl shadow-xl p-8 text-center">
    
    <!-- Status Code -->
    <div class="text-7xl font-bold text-gray-200 mb-4">
      <%= statusCode || 500 %>
    </div>

    <!-- Title -->
    <h1 class="text-2xl font-semibold mb-2">
      <%= title || "Something went wrong" %>
    </h1>

    <!-- Message -->
    <p class="text-gray-600 mb-6">
      <%= message || "We ran into an unexpected issue. Please try again or come back later." %>
    </p>

    <!-- Error ID (Production Friendly) -->
    <% if (errorId) { %>
      <p class="text-xs text-gray-400 mb-6">
        Error ID: <span class="font-mono"><%= errorId %></span>
      </p>
    <% } %>

    <!-- Actions -->
    <div class="flex flex-col sm:flex-row gap-3 justify-center">
      <a
        href="/"
        class="px-5 py-2.5 rounded-lg bg-black text-white text-sm font-medium
        hover:bg-gray-900 transition"
      >
        Go to Home
      </a>

      <button
        onclick="window.location.reload()"
        class="px-5 py-2.5 rounded-lg border text-sm font-medium
        hover:bg-gray-50 transition"
      >
        Try Again
      </button>
    </div>

    <!-- Support -->
    <p class="text-xs text-gray-500 mt-6">
      Need help?
      <a href="/contact" class="underline hover:text-black">
        Contact support
      </a>
    </p>

  </div>

</body>
</html>
