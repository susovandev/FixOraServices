<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <title>Technician Login | FixOra</title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    <script src="https://cdn.tailwindcss.com"></script>

    <style>
      body {
        background:
          radial-gradient(circle at 0% 0%, rgba(16, 185, 129, 0.07), transparent 40%),
          radial-gradient(circle at 100% 100%, rgba(15, 23, 42, 0.05), transparent 40%), #f8fafc;
      }
    </style>
  </head>

  <body class="min-h-screen flex items-center justify-center px-6">
    <%- include("../../partials/toast.ejs") %>
    <div
      class="w-full max-w-6xl grid md:grid-cols-2 bg-white border border-gray-200 rounded-3xl shadow-2xl overflow-hidden"
    >
      <!--  LEFT PANEL  -->
      <div class="hidden md:flex flex-col justify-center p-14 bg-slate-900 text-white">
        <h1 class="text-4xl font-bold mb-4">Welcome back to FixOra</h1>

        <p class="text-slate-300 mb-10">
          Login to manage bookings, track earnings, and grow your service business.
        </p>

        <div class="space-y-4 text-sm text-slate-300">
          <div class="flex items-center gap-3">
            <span class="w-8 h-8 rounded-full bg-emerald-500/20 flex items-center justify-center"
              >✔</span
            >
            Real-time booking alerts
          </div>

          <div class="flex items-center gap-3">
            <span class="w-8 h-8 rounded-full bg-emerald-500/20 flex items-center justify-center"
              >✔</span
            >
            Weekly secure payouts
          </div>

          <div class="flex items-center gap-3">
            <span class="w-8 h-8 rounded-full bg-emerald-500/20 flex items-center justify-center"
              >✔</span
            >
            Performance insights dashboard
          </div>
        </div>
      </div>

      <!--  RIGHT PANEL  -->
      <div class="p-10 md:p-14">
        <div class="max-w-md mx-auto">
          <h2 class="text-3xl font-semibold text-slate-900 mb-2">Technician Login</h2>

          <p class="text-gray-500 mb-8">Access your technician dashboard</p>

          <form action="/technician/login" method="POST" class="space-y-6">
            <!-- PHONE -->
            <div>
              <label class="block text-sm font-medium mb-1">Phone</label>
              <input
                type="text"
                name="phone"
                required
                placeholder="+91 XXXXXXXX20"
                class="w-full px-4 py-3 rounded-xl border border-gray-300 focus:ring-2 focus:ring-emerald-500 focus:border-emerald-500 outline-none"
              />
            </div>

            <!-- OPTIONS -->
            <div class="flex items-center justify-between text-sm">
              <label class="flex items-center gap-2 text-gray-600">
                <input type="checkbox" name="remember" class="rounded border-gray-300" />
                Remember me
              </label>
            </div>

            <!-- LOGIN BTN -->
            <button
              type="submit"
              class="w-full py-3 rounded-xl bg-slate-900 hover:bg-slate-800 text-white font-medium transition shadow-lg"
            >
              Login to Dashboard →
            </button>
          </form>

          <!-- OTP LOGIN -->
          <div class="mt-6 text-center">
            <a
              href="/technician/login-otp"
              class="text-sm text-emerald-600 font-medium hover:underline"
            >
              Login using OTP instead
            </a>
          </div>

          <!-- REGISTER -->
          <p class="text-sm text-gray-500 mt-6 text-center">
            New technician?
            <a href="/technician/register" class="text-emerald-600 font-medium hover:underline">
              Apply here
            </a>
          </p>
        </div>
      </div>
    </div>
    <%- include("../../partials/toast.ejs") %>
  </body>
</html>
