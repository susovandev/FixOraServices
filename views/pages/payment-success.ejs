<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Payment Successful | FixOra</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0">

  <script src="https://cdn.tailwindcss.com"></script>

  <style>
    body{
      background:
        radial-gradient(circle at 10% 10%, rgba(16,185,129,0.06), transparent 40%),
        radial-gradient(circle at 90% 90%, rgba(99,102,241,0.06), transparent 40%),
        #f8fafc;
    }
  </style>
</head>

<body class="min-h-screen flex items-center justify-center px-6">
  <%- include("../partials/toast.ejs") %>

<div class="max-w-2xl w-full bg-white rounded-3xl shadow-2xl border border-slate-200 p-10 text-center">

  <!-- SUCCESS ICON -->
  <div class="mx-auto w-24 h-24 rounded-full bg-green-100 flex items-center justify-center">
    <svg class="w-12 h-12 text-green-600" fill="none" stroke="currentColor" stroke-width="3"
      viewBox="0 0 24 24">
      <path stroke-linecap="round" stroke-linejoin="round"
        d="M5 13l4 4L19 7"/>
    </svg>
  </div>

  <!-- TITLE -->
  <h1 class="text-3xl font-bold text-slate-900 mt-6">
    Payment Successful 🎉
  </h1>

  <p class="text-slate-500 mt-3">
    Your booking has been confirmed. A technician will be assigned shortly.
  </p>


  <!-- ORDER INFO -->
  <div class="mt-8 text-left bg-slate-50 rounded-2xl p-6 border border-slate-200 space-y-3">

    <div class="flex justify-between text-sm">
      <span class="text-slate-500">Order ID</span>
      <span class="font-semibold"><%= orderId %></span>
    </div>

    <div class="flex justify-between text-sm">
      <span class="text-slate-500">Payment ID</span>
      <span class="font-semibold"><%= paymentId %></span>
    </div>

    <div class="flex justify-between text-sm">
      <span class="text-slate-500">Amount Paid</span>
      <span class="font-semibold">₹<%= amount %></span>
    </div>

    <div class="flex justify-between text-sm">
      <span class="text-slate-500">Service</span>
      <span class="font-semibold"><%= serviceName %></span>
    </div>

  </div>


  <!-- ACTION BUTTONS -->
  <div class="mt-10 flex flex-col sm:flex-row gap-4 justify-center">

    <a href="/bookings/<%= orderId %>"
       class="px-6 py-3 rounded-xl bg-slate-900 text-white font-medium hover:bg-slate-800 transition shadow-lg">
       Track Booking
    </a>

    <a href="/"
       class="px-6 py-3 rounded-xl border border-slate-300 text-slate-700 hover:bg-slate-100 transition font-medium">
       Back to Home
    </a>

    <a href="/invoice/<%= orderId %>"
       class="px-6 py-3 rounded-xl border border-indigo-500 text-indigo-600 hover:bg-indigo-50 transition font-medium">
       Download Invoice
    </a>

  </div>


  <!-- EXTRA NOTE -->
  <p class="text-xs text-slate-400 mt-8">
    Need help? Contact FixOra support anytime.
  </p>

</div>


<!-- OPTIONAL AUTO REDIRECT -->
<% if(autoRedirect){ %>
<script>
  setTimeout(()=>{
    window.location.href="/bookings/<%= orderId %>";
  },5000);
</script>
<% } %>

  <%- include("../partials/toast-script.ejs") %>
</body>
</html>